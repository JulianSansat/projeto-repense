<%= form_for(@classroom) do |f| %>
  <% if @classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
      <% @classroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entry_at %><br>
    <%= f.datetime_select :entry_at %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= collection_select(:classroom, :student_id, @students, :id, :name, {:prompt => false}) %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br>
    <%= collection_select(:classroom, :course_id, @courses, :id, :name, {:prompt => false}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
